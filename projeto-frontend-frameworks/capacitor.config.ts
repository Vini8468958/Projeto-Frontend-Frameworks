import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'projeto.frontend.frameworks',
  appName: 'projeto-frontend-frameworks',
  webDir: 'www'
};

export default config;
